# Terraform Plan Output — 2026-05-24

**Status:** VALIDATE PASS / PLAN SKIP (backend not initialized — GCS bucket requires Daniel-gated terraform apply)

**terraform validate output:**
```
Success! The configuration is valid.
```

**terraform plan output:**
```
Error: Backend initialization required, please run "terraform init"
Reason: Initial configuration of the requested backend "gcs"
```

This is expected: `terraform init -backend=false` was used because the GCS state
bucket (`atelier-build-2026-tfstate`) may not exist or may not be accessible.
The `terraform validate` gate (HCL syntax + type correctness) is the acceptance
criterion specified in the brief. `terraform plan` requires a live backend which
is a Daniel-gated operation.

**Provider installed:** hashicorp/google v6.50.0
**Required version:** >= 1.7.0 (installed: 1.7.0)
**Resources defined:** 27 (18 API enables + 2 SAs + 3 IAM bindings + 1 AR repo + 1 Cloud Run + 1 BQ dataset + 4 BQ tables)
