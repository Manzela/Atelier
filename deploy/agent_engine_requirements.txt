# Vertex AI Agent Engine sandbox requirements (AT-082, PRD v2.2 §12 E8).
#
# Pinned to exact versions matching atelier-core/requirements.lock so the
# deployed Agent Engine runtime resolves the same dependency set as the
# CI-verified build.  Update by regenerating requirements.lock and mirroring
# the four pinned versions here.
google-adk==2.1.0
google-genai==1.75.0
google-cloud-aiplatform==1.153.1
pydantic==2.13.4
